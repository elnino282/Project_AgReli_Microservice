# AUD-S3-003 — Loại duplicate SeasonsPage chết

**Trạng thái:** `FIXED` ngày 2026-08-21.

## Root cause

`src/pages/farmer/SeasonsPage.tsx` tự triển khai một season lifecycle và PHI panel song song nhưng không được import. Route sống dùng `features/farmer/seasons/SeasonManagement`; giữ hai implementation làm review nhầm và dễ drift contract.

## Acceptance contract

1. Xóa duplicate không reachable; route `/farmer/seasons` tiếp tục dùng `SeasonManagement`.
2. PHI core vẫn nằm ở harvest flow/backend gate; không chuyển UI chết sang route sống chỉ để giữ code.
3. Không còn reference `SeasonsPage`; full frontend regression/build xanh.

## Rollback

Khôi phục file bằng `git revert`; không có migration hoặc dữ liệu bị thay đổi.

## Verification

- `src/pages/farmer/SeasonsPage.tsx` đã xóa; không còn import/reference.
- `/farmer/seasons` vẫn map tới `SeasonManagement`; full frontend regression và build xanh.
