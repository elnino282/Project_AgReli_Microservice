# AUD-S3-002 — Mở route dữ liệu đất, nước và dinh dưỡng

**Trạng thái:** `FIXED` ngày 2026-08-21.

## Root cause

Ba workspace component đã hoàn chỉnh và có backend `FARMER`-protected, nhưng cả tab navigation lẫn route đều bị comment. Người dùng không thể nhập dữ liệu nền cho FDN từ route sống.

Runtime acceptance còn phát hiện dữ liệu seed/import có trước transactional outbox, vì vậy sustainability read-model thiếu farm/plot/season snapshot và ba route trả `ERR_PLOT_NOT_FOUND`. Backfill được thực hiện bằng outbox event additive trong service sở hữu dữ liệu, không đọc chéo database.

## Acceptance contract

1. Workspace hiển thị và điều hướng được ba tab `nutrient-inputs`, `irrigation-water-analyses`, `soil-tests`.
2. Mỗi route dùng đúng component hiện có, season ID từ URL và backend persisted contract.
3. Sáu contract/flow test FDN hiện hành cùng build/typecheck xanh.

## Rollback

Rollback code bằng `git revert`. Ba migration là forward-additive; khi cần rollback vận hành, giữ migration history và phát compensating event/migration thay vì sửa hoặc xóa migration đã chạy.

## Verification

- Ba route/tab được mở và dùng component persisted hiện có.
- `farm-service` V9 replay farm/plot và `season-service` V20 replay season qua transactional outbox; sustainability V4 chuẩn hóa source type legacy `LAB_TEST` thành `LAB_MEASURED`.
- Runtime read-model có snapshot và cả ba GET nutrient/water/soil của season seed trả HTTP 200 qua Vite proxy.
- Farm 27/27, season 21/21, sustainability 14/14 và FDN frontend 38/38 test xanh.
