# AUD-S3-004 — Khôi phục architecture gates

**Trạng thái:** `FIXED` ngày 2026-08-21.

## Root cause

`package.json` khai báo `check:fsd` và `check:legacy*` nhưng thư mục `scripts/` không tồn tại, nên CI/local command hỏng trước khi kiểm tra kiến trúc.

## Acceptance contract

1. `check:fsd` phát hiện dependency đi ngược `app > pages > widgets > features > entities > shared`; 28 exception cũ đã review được khóa trong baseline và gate chặn mọi vi phạm mới.
2. `check:legacy` đếm có cấu trúc; `--check` so với baseline đã review và `--update-baseline` là thao tác explicit.
3. Cả ba command chạy được và xanh trên cây code sau cleanup.

## Rollback

Rollback scripts/package/baseline bằng `git revert`.

## Verification

- `npm run check:fsd`: 1024 source file, 28 exception đã review, 0 vi phạm mới.
- `npm run check:legacy:baseline`: 0 legacy file/import trên baseline 0.
- Cả chế độ check và explicit `--update-baseline` đều có implementation thật trong `scripts/`.
