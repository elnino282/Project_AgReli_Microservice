# AUD-S3-006 — Hoàn tất boundary legacy frontend

**Trạng thái:** `FIXED` ngày 2026-08-21.

## Root cause

`src/components/` còn 55 wrapper/duplicate và `src/hooks/` còn một compatibility export. Hàng chục consumer tiếp tục import alias legacy dù implementation canonical đã nằm ở `shared`.

## Acceptance contract

1. Consumer dùng `shared` canonical; Markdown renderer, theme toggle và mobile hook được đặt trong shared boundary.
2. Không còn file hoặc import dưới `src/components`, `src/hooks`, `src/services`.
3. Legacy baseline bằng 0 và gate không cho tăng lại.
4. Full frontend test, typecheck, lint và build xanh.

## Rollback

Frontend-only; rollback bằng `git revert` patch mang mã `AUD-S3-006`.

## Verification

- Đã chuyển 76 import sang shared canonical, bổ sung canonical Markdown/theme/mobile hook và xóa 56 file legacy dưới `src/components`/`src/hooks`.
- `src/components`, `src/hooks`, `src/services` và import tương ứng đều bằng 0; legacy baseline 0/0.
- Full frontend 83 file/294 test, typecheck, lint 0 error và production build xanh.
